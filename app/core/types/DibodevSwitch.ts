/**
 * Type definitions for the DibodevSwitch component props.
 * @type {DibodevSwitchProps}
 * @property {boolean} modelValue - The current state of the switch (on/off).
 * @property {boolean} [disabled] - Optional property to disable the switch.
 */
export type DibodevSwitchProps = {
  modelValue: boolean
  disabled?: boolean
}
