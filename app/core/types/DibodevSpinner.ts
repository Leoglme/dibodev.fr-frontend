/**
 * Type definitions for the DibodevSpinner component props.
 * @type {DibodevSpinnerProps}
 * @property {number} size - The size of the spinner (in pixels).
 */
export type DibodevSpinnerProps = {
  size?: number
}
